{
  "name": "just-a-project-counter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rimraf dist || rm -rf dist"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "rimraf": "^5.0.5"
  }
}
